"use client";

/**
 * Internal component - provides space for cards to slide into when selected.
 * This component is intentionally simple and just reserves space.
 */
export function CardDisplayArea() {
  return <div className="flex-1" />;
}
