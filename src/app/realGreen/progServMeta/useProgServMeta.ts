import { useDispatch, useSelector } from "react-redux";
import { AppDispatch } from "@/store";
import {
  progServMetaActions,
  progServMetaSelect,
} from "@/app/realGreen/progServMeta/progServMetaSlice";

export function useProgServMeta() {
  const dispatch = useDispatch<AppDispatch>();



  return;
}
