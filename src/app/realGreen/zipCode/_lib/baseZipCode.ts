import { ZipCode } from "./ZipCodeTypes";
import {baseStrId} from "@/app/realGreen/_lib/realGreenConst";

export const baseZipCode: ZipCode = {
  zip: baseStrId,
  city: baseStrId,
  createdAt: "",
  updatedAt: "",
};
