1. ProductDocPropsModel.ts
   1. Currently only store an array of product ids
   2. Needs to be expanded to include rate of consumption for each sub-product per master unit.
   3. The sub-product unit is pre-defined, so we only need to store a number.
2. Hydration