// hydrateLastAssigned is no longer used — service.assignments (AssignmentUtils) is hydrated
// directly in centralSelectors.ts from the assignment Redux store.
// This file is kept as a placeholder to avoid breaking any imports that may reference it.
// It can be deleted once all consumers have been confirmed to use service.assignments directly.
export {};
